spring:
  profiles:
    active: local

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    initialization-mode: never
    url: jdbc:mysql://localhost:3306/ever?serverTimezone=UTC&characterEncoding=UTF-8
    username: ever
    password: asdqwe1!
---
spring:
  profiles: local

logging:
  level:
    com:
      naru:
        katalk: info